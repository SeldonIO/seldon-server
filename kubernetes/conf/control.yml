apiVersion: v1
kind: Pod
metadata:
  name: seldon-shell
spec:
  containers:
  - image: seldonio/pyseldon
    name: seldon-shell
    command: [ "sleep", "infinity" ]
    volumeMounts:
    - mountPath: /root/.seldon
      name: data-volume
  volumes:
    - name: data-volume
      emptyDir: {}
