#!/bin/bash

set -o nounset
set -o errexit

STARTUP_DIR="$( cd "$( dirname "$0" )" && pwd )"

export BOOTSTRAP_DIR=/vagrant
export BOOTSTRAP_DOCKER_USER=vagrant
export BOOTSTRAP_TOMCAT_HOME=/home/vagrant/apps/tomcat

#${BOOTSTRAP_DIR}/bootstrap_scripts/install-locale.sh
##${BOOTSTRAP_DIR}/bootstrap_scripts/install-linux-image-extra.sh
#${BOOTSTRAP_DIR}/bootstrap_scripts/install-docker.sh
#${BOOTSTRAP_DIR}/bootstrap_scripts/install-vim.sh
#${BOOTSTRAP_DIR}/bootstrap_scripts/install-jdk7.sh
#${BOOTSTRAP_DIR}/bootstrap_scripts/install-tomcat.sh
#${BOOTSTRAP_DIR}/bootstrap_scripts/install-bash-profile.sh

